# Archived specs

Finished work units live here so `specs/` only shows what is current.

Do not create archive files for Phase 0–11. That history stays in `context/roadmap-history.md` / `context/decisions.md`.
