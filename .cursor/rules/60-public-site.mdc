---
description: Public site — home, about, blog, tools. No private practice data.
globs: apps/web/app/(public)/**
---

# Public site

As-built: @context/domains/public-site.md · tools: @context/domains/tools.md

`(public)/` is home, about, blog, tools, legal — not a single page. Same tokens from
`packages/tokens`. Mobile-first.

**No private data here** — never session logs, reflections, or coach chat.
`/tools` is utilities, not gear (gear is `/about#kit`) and not skill-improvement gamification.
No Chordify-style icon-tile grids.
