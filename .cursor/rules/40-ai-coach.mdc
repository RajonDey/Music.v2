---
description: AI music coach — API, streaming, behavior.
globs: apps/web/app/api/coach/**,apps/web/lib/coach-prompt.ts,apps/web/components/coach/**
---

# AI Coach

Central feature, not a side panel. Full spec: @docs/AI_COACH.md

- `POST /api/coach` — middleware-protected, streams via Vercel AI SDK + `gemini-2.0-flash`.
- System prompt in `apps/web/lib/coach-prompt.ts`. Context built server-side from Supabase.
- Persist to `coach_messages`. Warm mentor tone; never shame or score.
- `GOOGLE_GENERATIVE_AI_API_KEY` server-side only.
